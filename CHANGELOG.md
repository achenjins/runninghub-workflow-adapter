# 更新日志

## v1.2.0

- 重构了配置页面
- 支持在聊天里直接用自然语言生成或修改媒体：文生图、图生图、视频、音频和文件都能触发
- 当前消息或引用消息里带的图片会自动用作参考图：只有一个空着的图片输入时自动绑定，多张图按用途绑定，不会瞎猜
- 多张图没说清各张用途时会先问一句；缺必填内容会引导补齐，不会直接提交
- 可以在上次结果上接着改，比如「上次那张把背景换成蓝色」，插件会沿用原任务的提示词和约束
- 支持用当前 QQ 头像、群头像当参考素材，比如「画我」「用我头像」
- 输入节点的素材用途可以自己命名，示例配置里把两张图分别标成首帧、尾帧；配好后自然语言就能按用途绑定
- 视频、音频目前只做上传和用途绑定，不解码内容
- 插件按用户和会话记住最近用过、生成的图片，默认 5 张（可设 0～20，设 0 关闭并清空），同一图片自动去重，每张图带一句最多 50 字的简介
- 结果发回后，机器人会顺着当前聊天语气再回一句，方便接着提要求
- 支持用自然语言管任务：取消当前会话里的任务，或把某个任务里没发出的结果再发一次（补发针对整个任务）
- 每个工作流可单独关掉「允许聊天调用」，关掉后仍可用命令运行
