# 麦麦画师 · RunningHub

![running](https://count.kjchmc.cn/get/@rhaajinse?theme=gelbooru)
![Version](https://img.shields.io/badge/version-1.2.0--test-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Platform](https://img.shields.io/badge/platform-NapCat%20%2F%20QQ-orange)

让麦麦亲手给你画画：从一句话文生图，到多参考图 / 视频生成，统统自动完成并把成品发回聊天。支持自定义工作流、自己优化成本、AI 扩写提示词和限流防刷。仅适配 NapCat / QQ。

平台入口：[RunningHub 国外](https://www.runninghub.ai?inviteCode=bvhsaqdr) / [RunningHub 国内](https://www.runninghub.cn?inviteCode=8cq8uhl8)

> [!WARNING]
> 本插件目前**仅适配 NapCat 的 QQ**，其余平台 / 适配器未经测试，可能无法正常使用。

> [!IMPORTANT]
> RunningHub 是付费平台，每跑一次任务都会消耗你的账户余额 / 积分。建议装好后先配置「访问控制」（见文末），限制谁能用、每小时最多跑几次，避免被群里的人刷爆余额。

---

## 目录

- [安装](#安装)
- [三步跑通第一张图](#装好之后三步跑通第一张图)
- [提示词扩写（可选）](#提示词扩写可选)
- [整理输入节点](#识别之后整理你的输入节点)
- [常用命令速查表](#常用命令速查表)
- [自然语言触发](#自然语言触发)
- [小白必懂的几个概念](#小白必懂的几个概念)
- [完整配置项参考](#完整配置项参考)
- [进阶玩法](#进阶玩法)
- [常见问题 FAQ](#常见问题-faq)

---

## 这个插件能做什么

- ✅ **自然语言生成与修改**：文生图、图生图、视频、多参考图和可编辑参数
- ✅ 支持**文生图 / 图生图 / 文生视频 / 多参考生视频**等各类工作流，可配置多个
- ✅ **适配性**：简单的出图、出视频工作流均可适配
- ✅ **低成本**：出高清二次元 2K 图，一张图仅 0.05 元
- ✅ **高自由度**：runninghub是可自定义各种工作流的平台，不受配置限制，想出啥图出啥图
- ✅ 按配置好的**自定义提示词规则自动调用 LLM 扩写**，一句话即可出高质量图
- ✅ 两种触发方式：**命令**、**自然语言**；实时查询工作流和当前／引用／近期／上次生成的素材
- ✅ 结果自动发送；可选「发完自动撤回」，保护你的隐私（仅 NapCat / QQ 适配器生效）
- ✅ 在 RH 海外平台配置好**鸭鸭加密**，可发送鸭鸭加密图（自行搜索鸭鸭加密内容，不提供教程）

---

## 安装

    cd <MaiBot目录>/plugins
    git clone https://github.com/achenjins/runninghub-workflow-adapter.git runninghub-workflow-adapter
    pip install -r runninghub-workflow-adapter/requirements.txt

克隆并装好依赖后，重启 MaiBot（或在 WebUI 里热重载插件），插件即自动加载。

---

## 装好之后：三步跑通第一张图

### 第 0 步：确认插件已加载

安装后重启 MaiBot，在聊天里发一句：

    /工作流

- 如果回复「尚未配置任何工作流」→ 插件已成功加载，继续下一步。
- 如果没反应 → 去 MaiBot 的插件 / 日志页面确认插件是否启用、有没有报错。

### 第 1 步：注册并填 API Key

这是最重要的一步，不填后面全都会失败。

**① 注册账号（用邀请码注册，可得 500 RH 币）**

打开对应平台的注册链接（邀请码已内置在链接里）：

- 国内站：[注册国内账号](https://www.runninghub.cn?inviteCode=8cq8uhl8)
- 海外站：[注册海外账号](https://www.runninghub.ai?inviteCode=bvhsaqdr)

**② 获取 API Key**

注册并登录后，打开对应平台的 API 页面，复制页面里的 API Key：

- 国内站：[打开国内 API 页面](https://www.runninghub.cn/enterprise-api/consumerApi?inviteCode=8cq8uhl8)
- 海外站：[打开海外 API 页面](https://www.runninghub.ai/enterprise-api/consumerApi?inviteCode=bvhsaqdr)

**③ 填进插件**

打开 MaiBot 的**插件配置页**，找到本插件的「RunningHub 服务」一栏：

- 用国内站（runninghub.cn）→ 把 Key 填进「国内 API Key」
- 用海外站（runninghub.ai）→ 把 Key 填进「国外 API Key」

两边账号和 Key 不通用，用哪个填哪个，也可以都填。

> [!TIP]
> 填完通常需要「重载插件」或「保存配置」才生效。

### 第 2 步：识别（导入）一个体验工作流

先给大家一个我**删除了内部高成本豆包提示词优化**的**文生图体验工作流**（注意：打开不能直接使用，没有提示词优化节点，想体验原版可去下方 up 主的视频里找完整版工作流）：

- 海外版：[海外体验工作流](https://www.runninghub.ai/zh-cn/workflow/2087492768787685378?inviteCode=bvhsaqdr)
- 国内版：[国内体验工作流](https://www.runninghub.cn/workflow/2087939838371786753?inviteCode=8cq8uhl8)

> [!NOTE]
> 这个工作流魔改自 B 站视频教程 [BV1arGt6wExL](https://www.bilibili.com/video/BV1arGt6wExL)，感谢 up 主 [@每日提钢小助手5号](https://space.bilibili.com/3690999272442168) 分享的工作流，如侵权可联系删除。该up工作流自带提示词优化，会消耗rh上的余额

> [!IMPORTANT]
> 工作流打开保存后可进行修改，保存后的工作流 ID（链接里的数字）会变成你账号专属的 ID；使用识别命令时记得换成对应 ID。

配置好apikey后直接在聊天里发（按你的平台二选一）：

    格式为：/识别[国外|国内]工作流 工作流ID 工作流自定义名称
    
    例子： /识别国外工作流 2087492768787685378 文生图
    
          /识别国内工作流 2087939838371786753 文生图

看到「识别成功」即导入完成。

**导入后，做两件事**：

1. **检查输入节点**：进入 MaiBot 插件配置页 → 工作流列表，核对「文生图」的主提示词节点。其余需要用户修改的参数可保留为 `text` 并填默认值，自然语言调用会沿用默认值。无需暴露的固定参数可以在 RunningHub 中保存，或配置成 `default`。
2. **配置专属提示词模板**：将这个工作流的「启用 LLM 扩写」打开，并在「扩写模板路径」里填 prompt/anima3_prompt_template.txt（这个模板文件已随插件一起提供，预置在仓库的 prompt/ 目录下）。

### 第 3 步：跑一张图

    /rh运行 文生图 原神刻晴
    
    /rh运行 文生图 原神刻晴与甘雨，室内

- 如果这个工作流还需要**参考图 / 参考音频**，按提示发送文件。只有设置了 `required = false` 的可选输入可以「**跳过剩余**」，必填输入需要补齐。
- 如果识别出了一些**可修改参数**（如分辨率），机器人会问你「可修改：1.宽度=512 …」，直接回复新值如：0.5 128 15.0
- 回「**不变**」直接用默认值。

**设备与费用参考**（该文生图工作流）：

- 推荐使用默认的 **Standard** 设备。
- 出图约 **2 分钟**。
- 平均消耗 **25 RH 币**（开会员约合 **0.04 元**）。
- 注：**Ultra** 设备仅会员可用，且消耗较大。

然后等一会儿，结果会自动发回聊天窗口。🎉

---

## 提示词扩写（可选）

开启后，AI 会把你写的内容扩写成详细提示词，出图质量更高。

**作用**：开启后，你写「一只猫」就够，插件会先按你的模板把它扩写成一段完整的英文提示词再提交给工作流；不开启则你写什么就原样传什么。

**怎么开（两步）**：

> [!TIP]
> 如果你用的是第 2 步的「文生图」体验工作流，专属模板已随插件提供（prompt/anima3_prompt_template.txt），可**跳过第 1 步**，直接把「扩写模板路径」填成 prompt/anima3_prompt_template.txt 即可。

1. 在插件目录下新建一个模板文件，比如 templates/my_template.txt，内容写清楚「要 AI 怎么扩写」，你可以查找对应模型的提示词模板，文档或skill，让ai给你总结一份提示词，一定要与使用的模型对得上，越详细写的越好，扩写模型建议用deepseekV4flash，支持txt/md文件。一个简短示例：

       你是一位专业的 Stable Diffusion 提示词工程师。
       请把用户需求扩写成一段详细的英文提示词，必须包含：
       主体描述、画质词、构图、光影、风格、镜头。
       只输出提示词本身，不要解释。（这句话很重要）

2. 打开 MaiBot 插件配置页，找到对应工作流，把「启用 LLM 扩写」打开，并在「扩写模板路径」里填 templates/my_template.txt（相对插件目录的相对路径）。

保存后，再发 /rh运行，提示词就会先自动扩写再出图了。

**默认调用哪个模型**：扩写默认使用宿主的 `utils`。在配置页「辅助功能 → 提示词扩写模型」下拉选择 `replyer`、`planner`、`utils` 或 `vlm` 即可。扩写完成后，结果会填入主提示词节点，再提交生成。

---

## 识别之后：整理你的输入节点

<details>
<summary>展开查看：节点怎么留、怎么删、漏了怎么补</summary>

用 /识别工作流 导入后，插件会自动列出一串「输入节点」。这些节点是插件按常见规则猜出来的，不一定完全符合你的需求，建议看一眼再正式用。

去哪看：**MaiBot 插件配置页 → 工作流列表 → 对应工作流 → 输入节点**。

### 哪些要保留、哪些可以删

按节点类型判断即可：

| 节点类型 | 建议 |
|---------|------|
| **提示词**（prompt） | **必需保留**。这是你每次输入描述提示词的核心位置，也是提示词按规则扩写后自动输入的位置。一个工作流仅能同时存在一个节点为prompt节点 |
| **图片 / 音频 / 视频** | 需要传参考文件的就**保留**；不需要就删掉，参考文件固定可在工作流中传一个固定文件保存 |
| **可修改的配置参数**（text） | 每次跑之前都会问你的参数（分辨率、步数、CFG、种子、画师串提示词等）。**想自己控制就保留；嫌每次被问烦就删掉**；想固定住就改成「default」并填好值。建议修改不频繁的如分辨率就直接删了到rh平台上改 |
| **固定默认值**（default） | 一般**保留**，直接用识别出的默认值。或者删掉，用工作流中默认值 |

> [!WARNING]
> 节点类型别填错：插件靠它判断这个节点需要的是图片、音频还是视频参考文件，用来引导你上传、区分文件种类。填错了会导致参考文件收错、混乱、甚至工作流报错。

**简单判断**：
- 你每次都要变的 → 留成「提示词 / 图片 / 音频 / 视频 / 可编辑配置」
- 永远不动的 → 留成「固定默认值」，或干脆删掉
- 用不到的 → 直接删

> [!NOTE]
> 删节点只是让插件不再跟这个节点交互，不影响 RunningHub 上工作流本身。

### 识别漏了节点，怎么自己加

普通识别只认常见节点，遇到自定义节点可能漏掉。补法有两种：

1. **先用 /详细识别工作流 重新导一遍**，它能识别更多参数（步数、采样器、CFG、种子、lora 强度等）。
2. **手动加**：在「输入节点」里点添加，每个节点填 5 项。

![视频画幅和分辨率节点](docs/示例图片1.png)

以上图「视频画幅和分辨率」节点（右上角 `#115`）为例：

| 字段 | 填什么 |
|------|--------|
| 节点 ID | 节点右上角的编号，如 `115` |
| 字段名 | 要控制的那一个参数名，如 `aspect_ratio`（宽高比）/ `megapixels`（像素）/ `multiple`（倍数） |
| 输入内容 | 该参数的默认值，如 `9:16` / `0.2` / `32`；留空 = 运行时由用户提供 |
| 节点类型 | 固定不变填 `default`；每次想改填 `text` |
| 输入说明 | 可以自由填，给用户看的中文提示 |

> [!IMPORTANT]
> 一个节点只能填一个字段名。想同时控制宽高比、像素、倍数，就加三行，节点 ID 都填 `115`，字段名分别填 `aspect_ratio`、`megapixels`、`multiple`。

**节点 ID 和字段名从哪看**：打开 RunningHub 网站上的工作流，每个节点都有一个编号（就是「节点 ID」），节点上要填内容的参数名就是「字段名」。图中右侧 `width` / `height` 是输出口，不是要填的输入。实在找不到，就先用 /详细识别工作流 自动填好，再照着改。

</details>

---

## 常用命令速查表

| 命令 | 作用 | 例子 |
|------|------|------|
| **/rh运行 工作流名 描述提示词** | 运行工作流生成图片 / 视频 | /rh运行 文生图 原神刻晴 |
| **/rh中断** | 中断任务：交文件阶段直接结束；已提交则按编号取消 | /rh中断 |
| **/工作流** | 列出已配置的工作流 | /工作流 |
| **/识别国外工作流 ID 名称** | 导入 runninghub.ai 上的工作流（关键节点） | /识别国外工作流 2087... 文生图 |
| **/识别国内工作流 ID 名称** | 导入 runninghub.cn 上的工作流（关键节点） | /识别国内工作流 2087... 文生图 |
| **/详细识别国外工作流 ID 名称** | 导入国外工作流，并识别**全部**参数 | /详细识别国外工作流 2087... 文生图 |
| **/详细识别国内工作流 ID 名称** | 导入国内工作流，并识别**全部**参数 | /详细识别国内工作流 2087... 文生图 |

> [!NOTE]
> **「识别」和「详细识别」的区别**：
> - 普通识别：只认最常用的（提示词、图片、音频、视频、分辨率、长宽比），对大多数场景够用。
> - 详细识别：还会把步数、采样器、CFG、种子、lora 强度等参数也识别出来，变成「每次跑之前可以改」的配置，适合想精细调参的人。

---

## 自然语言触发

配置好工作流用途和输入节点后，直接在群里／私聊提出需求：

    帮我画一张甘雨，蓝色长发，全身立绘

也可以发送或引用图片后说「把这张图改成水彩，保留人物和构图」，发送两张图后说「第一张作主体，第二张作风格」，或要求「根据上次生成结果，把背景改成蓝色」。图生视频工作流可配置 `first_frame`、`last_frame` 等输入角色。音频和视频支持素材绑定与上传，目前不自动解码其内容。

简单请求可直接调用 `run_workflow`：已知工作流，或按成品类型和素材需求筛选后只有一个候选时，跳过额外的选流模型调用。需要挑选工作流或区分多份素材时再用 `rh_context`；只有尚未看过且需要辨认内容时才用 `rh_inspect_media`。`references` 按输入标识绑定素材 ID，`parameters` 只能修改配置为 `text` 的节点。唯一当前／引用素材和唯一缺失输入可自动配对；多图用途不明确时会询问。缺项或绑定出错会直接返回上下文、工作流输入 key 和候选素材 ID，便于直接补正。默认值自动保留，补充需求和继续修改上次任务分别使用草稿和任务 ID 保留原约束。

开始时由插件代发一句短提示，模型可在同一次工具调用的 `start_message` 中按当前聊天语气填写，未填时使用「好，我来画。」等短句，不再展示排队编号。成功接手后结束本轮，插件后台发送成品，再触发一次简短结果回复。相同触发消息、用户、会话、工作流 ID 和区域只记录一次自然语言任务；重复调用不会再次付费生成或重发开始提示。新消息可以发起新任务。

「聊天生成 → 最近图片数量」默认 **5 张**，可设为 0～20，填零关闭并清空。插件保存当前用户、当前会话实际观察或使用过的参考图，以及已发出的生成图片，并给每张图保留约 **50 字简介**。`rh_context` 会把简介和最近使用顺序放进素材候选，模型先凭简介选图；重复观察已有记录时直接返回简介，不再次读原消息或调用视觉模型。同一图片按内容去重，再次使用会移到最近位置；插件重启后仍能复用保存的原图，不依赖原消息或临时下载链接。

首次记录图片时会使用已选视觉模型生成一次简介，已有摘要会复用；摘要失败仍保留原图，提示根据对话或询问用户，不编造图片内容。图片记录写入失败不阻断生成或发送。视频和语音不保存到这份记录，也不新增识别调用，依据对话记忆或向用户确认。

- 输入角色建议填写 `subject`、`style`、`background`、`first_frame`、`last_frame`；输入标识 `input_key` 留空时使用 `节点ID.字段名`。
- `description`、`capability`、`output_type` 帮助选择工作流。设置 `natural_language = false` 可关闭单个工作流的自然语言入口。
- 自动扫描的历史素材只来自当前用户、当前会话；明确引用可使用同一会话其他人的素材。普通历史默认保留一小时、最多返回 12 项；已保存的最近图片另计，按使用顺序和数量淘汰。
- 「聊天生成 → 视觉模型」默认选择宿主视觉模型（`vlm`），可通过下拉框更换；识图失败时继续使用原图。没有视觉解析结果时不会声称已看图。
- 用户和会话身份取自宿主上下文，不再让 LLM 填 QQ 号；无法识别身份时拒绝执行。
- 配置热更新后，`rh_context` 立即返回最新工作流。首次升级增加了新工具及消息读取能力，需要重载插件。

完整配置和服务器验证方法见 [测试部署指南](docs/TESTING.md) 与 [多参考图配置示例](examples/natural-language.toml)。
本轮通知、取消、去重及群聊路由的审查结论见 [2026-09-09 审查记录](docs/AUDIT-2026-09-09.md)。

---

## 小白必懂的几个概念

<details>
<summary>展开查看：工作流 ID / 区域 / 设备 / 节点类型</summary>

### 1. 工作流 ID 从哪找？

在 RunningHub 网站上打开工作流页面，看浏览器**地址栏**，里面有一长串数字（通常 19 位左右），那就是工作流 ID。把它复制下来即可，工作流保存后id唯一，不会改变。

### 2. 国内 / 国外怎么选？

- runninghub.ai 是国外站，runninghub.cn 是国内站，**账号和 API Key 不通用**。
- 工作流在哪个站打开的，就用哪个站的命令识别、填哪个站的 API Key。
- 如果填了 Key 却报「对应 API Key 未填写」，多半是区域和工作流对不上。

### 3. 设备类型选哪个？

导入时默认是 **Standard**（标准）。一般情况保持默认即可：

- **Standard** —— 标准，最常用，推荐
- **Plus** —— 更快 / 更高配/建议生视频时用
- **Ultra** —— 高配档位，**仅会员可用，消耗较大**

改设备类型：在 MaiBot 插件配置页「工作流列表」里，找到对应工作流的「设备类型」下拉改一下即可。

### 4. 节点类型是什么？

导入后，每个工作流会有一串「输入节点」，代表这个工作流要喂给它什么。常见几种：

| 类型 | 一句话解释 | 运行时行为 |
|------|-----------|-----------|
| **提示词**（prompt） | 你要生成什么 | 优先接收 `/rh运行` 后面的描述 / LLM 扩写文本；没输入时回退节点默认值；无默认值且没输入时会先询问描述；**整个工作流仅支持一个提示词输入节点** |
| **图片 / 音频 / 视频** | 要上传的参考文件 | 留空会引导你上传；填了默认值就直接用 |
| **可编辑配置**（text） | 每次可改的参数（如分辨率） | 自然语言默认沿用已有值，只覆盖明确指定的参数；命令交互仍提供配置确认 |
| **固定默认值**（default） | 固定参数（如分辨率、长宽比） | 直接用，不问你；没填默认值就跳过 |

> [!NOTE]
> 如果工作流有多个提示词输入（如正 / 负提示词），只保留一个「提示词」（prompt），其余提示词改用「可编辑配置」（text）——扩写功能只会适配传入「提示词」类型的节点。

> [!TIP]
> 这些不用手动去理解原理——用 /识别工作流 导入后基本就自动填好了，需要微调再去插件配置页改。

</details>

---

## 完整配置项参考

<details>
<summary>展开查看：所有配置项与默认值</summary>

下面是插件配置页里的所有配置项，**绝大多数保持默认即可**，用到再改。

### 平台连接

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 国外密钥 | 空 | 从国外平台个人中心获取 |
| 国内密钥 | 空 | 从国内平台个人中心获取 |

### 任务设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 同时运行数 | 2 | 远端未结束的任务也占名额 |
| 最多排队数 | 10 | 运行名额之外允许等待的数量 |
| 文件上限（兆字节） | 64 | 单个输入或结果文件的大小上限 |
| 查询间隔（秒） | 15 | 查询远端任务进度的间隔 |
| 单轮查询时长（秒） | 1800 | 超时后继续跟踪，不重复生成 |
| 请求超时（秒） | 120 | 平台请求及文件传输的超时时间 |
| 查询重试次数 | 3 | 一轮查询中网络错误的重试次数 |
| 发送重试次数 | 2 | 明确失败才自动重试，状态未知时手动补发 |

### 辅助功能

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 智能识别节点 | 开启 | 识别失败时改用节点规则 |
| 节点识别模型 | `utils` | 用于识别工作流输入与参数 |
| 提示词扩写模型 | `utils` | 各工作流可单独开启扩写 |
| 自动撤回结果 | 关闭 | 发送结果后自动撤回 |
| 撤回延迟（秒） | 90 | 填零不撤回 |

### 使用权限

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 允许的用户 | 空 | 留空允许所有用户 |
| 允许的群 | 空 | 留空允许所有群，私聊不受此项限制 |
| 每人每小时次数 | 0 | 包含排队任务，填零不限次数 |
| 管理员 | 空 | 可导入工作流、管理当前会话任务 |
| 仅管理员导入 | 开启 | 限制通过聊天识别并保存工作流 |

> [!WARNING]
> 默认**全部放行**——任何能跟你机器人聊天的人都能触发任务（花你的钱）。建议至少填一个：要么加白名单，要么设个频率上限。

### 工作流

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 工作流名称、编号 | 空 | 对应平台工作流，用于选择和运行 |
| 平台区域 | 国外 | 国外或国内 |
| 运行设备 | 标准 | 标准、增强、旗舰 |
| 用途说明 | 空 | 简要描述适用场景 |
| 工作流用途 | 自动判断 | 文字生图、图片编辑等用途 |
| 结果类型 | 图片 | 图片、视频、音频、文件 |
| 允许聊天调用 | 开启 | 关闭后仍可通过命令运行 |
| 扩写提示词 | 关闭 | 将用户描述整理为生成提示词 |
| 扩写方式 | 自动选择 | 自动选择、图片生成、图片编辑、视频生成、保留原文 |
| 自定义模板 | 空 | 相对于插件目录的模板文件路径 |
| 耗时与费用说明 | 空 | 供机器人选择时参考 |
| 输入节点 | 空 | 按需增加节点，或通过聊天识别导入 |

输入节点使用中文标题和下拉选项，包括输入名称、节点编号、字段名、输入类型、默认值、素材用途和参数校验等。「必须提供内容」默认关闭，开启后才要求补齐没有默认值的输入。只有「固定默认值」类型不允许工具修改。

### 聊天生成

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 启用聊天生成 | 开启 | 允许通过自然语言生成或修改媒体 |
| 工作流选择模型 | `utils` | 多个候选且未指定名称时使用 |
| 视觉模型 | `vlm` | 对应宿主的视觉模型，可下拉更换 |
| 最近图片数量 | 5 | 每人每个会话保存的原图及约 50 字简介，0～20，填零关闭并清空 |
| 允许使用头像 | 开启 | 把当前用户和群头像加入素材候选 |
| 参考消息数 | 20 | 从当前会话读取的最近消息条数 |
| 素材候选上限 | 12 | 普通素材候选数量，已保存的最近图片另计 |
| 素材保留时间（秒） | 3600 | 普通素材、待补需求及临时识图缓存的保留时间，不影响已保存图片 |
| 模型超时（秒） | 45 | 工作流选择、识图和扩写的等待上限 |

模型下拉选项仅有 `replyer`、`planner`、`utils`、`vlm`，按此顺序显示，配置文件也保存这四个原始名称。之前的「回复模型」「规划模型」「通用模型」「视觉模型」兼容读入，保存时自动转换为对应原始名称。配置页字段名称和说明保持中文，工作流等其他下拉选项仍使用中文；任务快照和平台请求使用原始标识。完整配置见 `config.example.toml`。

</details>

---

## 进阶玩法

<details>
<summary>展开查看：访问控制与自动撤回</summary>

### 限制谁能用 / 防刷爆余额

在配置页「访问控制」里：

- **只让指定用户用**：allow_users 填允许的用户 ID（每行一个）。
- **只让指定群用**：allow_groups 填群号。
- **限频**：max_per_user_per_hour 填一个数，比如 5，每人每小时最多 5 次。

配置后，/rh运行、/识别工作流、/工作流 这些命令都会统一受控。

### 自动撤回（仅 NapCat / QQ）

如果你用的是 NapCat 适配器，把「自动撤回」打开、设个秒数，图片发出去后会自动撤回。

</details>

---

---

## 项目结构（v1.2.0 测试版）

插件入口负责配置、命令和宿主适配；主要逻辑位于 `rh_generic_lib/`。自然语言工具模块使用 MaiBot SDK，其余核心模块采用可注入接口：

- `workflow_runner.py`：节点类型解析、nodeInfoList 构建、结果 URL 分类、消耗解析
- `session_machine.py`：交互式收集会话数据结构与查找规则
- `file_source.py`：消息文件提取、base64 / URL / 本地路径读取
- `delivery.py`：NapCat 直发、回退发送、定时撤回
- `task_journal.py`：任务日志持久化
- `task_runtime.py` / `task_queue.py`：持久化排队、额度预约、并发限制、恢复与补发
- `media_context.py`：当前、引用、近期和生成结果素材的会话隔离及读取
- `media_plan.py`：工作流能力、角色绑定、默认值和参数校验
- `natural_language.py`：实时上下文、看图、运行、任务管理工具

任务日志默认写入插件 `runtime_dir/task_journal.json`。先持久化 `queued` 和额度预约，再执行上传与提交；远端任务 ID 单独记录。生成状态与发送状态分开保存，重启后恢复查询及确定未发送的结果；已发送项不会重复补发。提交响应丢失记为 `unknown_submission`，不会自动重新提交。日志损坏或无法写入时拒绝新付费任务。

`/rh状态 [任务ID]` 查看状态，`/rh中断` 取消自己的任务，`/rh补发 任务ID` 只补发尚未确认发送的结果。发送状态未知时应先确认是否已收到，再手动补发；网络条件下无法保证发送严格恰好一次。管理员确认 RunningHub 记录后，可用 `/rh核对 本地任务ID 远端任务ID` 关联任务，或 `/rh核对 本地任务ID 未创建` 释放保留名额。

## 常见问题 FAQ

<details>
<summary>展开查看：常见问题排查</summary>

**Q：跑一次要花钱吗？**
A：要。RunningHub 按任务消耗余额 / 积分，每跑一次都会扣。所以建议先配「访问控制」。

**Q：识别命令报「没有使用本插件的权限」？**
A：管理员在「访问控制」里设了白名单，但你的用户 ID 不在里面。找管理员加上，或改用受限的入口。

**Q：报「对应 API Key 未填写」？**
A：这个工作流是国内的（domestic），但你没填国内 Key（或反过来）。检查 [server] 的 Key 和工作流的 region 是否对得上。

**Q：识别结果为空 / 识别不出节点？**
A：普通识别只认关键节点，有些自定义节点认不到。改用 /详细识别工作流，或去插件配置页手动加节点。

**Q：上传文件后没反应？**
A：确认插件已重载；看日志有没有「已接收输入」/「任务已提交」。新版走的是 Hook 通道，旧版事件已停用。

**Q：提示词扩写没生效？**
A：确认 `llm_enhance` 已开；自定义模板路径应指向存在的文件，未设置模板时使用 `prompt_profile` 内置策略。检查扩写模型槽位是否可用；`raw` 策略在没有自定义模板时保留原文。

**Q：为什么我说话没触发生成？**
A：确认已启用自然语言、工作流用途及节点类型正确，并允许 `rh_context`、`rh_inspect_media`、`run_workflow`、`rh_task` 工具。首次升级后重载插件；之后工作流配置随热更新生效。没有可信消息身份或素材已过期时，按返回提示重新发送需求／文件。

**Q：结果图片想让它自己删掉？**
A：这是「自动撤回」功能，仅 NapCat / QQ 适配器可用，在「功能设置」里开启并设秒数。

</details>

---

## 许可证

MIT
